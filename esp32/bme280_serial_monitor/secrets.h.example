// Copy this file to secrets.h and fill in the values below.
// Do not commit secrets.h.

const char WIFI_SSID[] = "your-wifi-network-name";
const char WIFI_PASSWORD[] = "your-wifi-password";

// These must exactly match DEVICE_ID and DEVICE_SECRET in the root .env file.
const char DEVICE_ID[] = "weather-station-001";
const char DEVICE_SECRET[] = "replace-with-the-matching-device-secret";

// Use the computer running `npm start` on the local network, not localhost.
const char BACKEND_URL[] = "http://192.168.1.50:3000";
